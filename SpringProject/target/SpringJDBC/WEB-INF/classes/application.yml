spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost/tutoSpringDB
    username: postgres
    password: postgres

---

spring:
  profiles: test
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost/tutoSpringDB
    username: postgres
    password: postgres
